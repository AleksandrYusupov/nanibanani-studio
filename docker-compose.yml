services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      DATA_DIR: /app/data
      LOCAL_STORAGE: "true"
    volumes:
      - app_data:/app/data

volumes:
  app_data:
